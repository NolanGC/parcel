// Bumped by hand when the conversion rules change in a way worth separating
// in the hillclimb ledger. The dashboard also records a content hash of this
// directory, so the version is the human-readable half of that pair: the hash
// says "different rules", the version says "different enough to care".
export const ZEN_VERSION = "0.1.0";
